#include <iostream>
using namespace std;
int ncr(int n,int r )
{
    if (n < r)
          return 0;
    if(r==0||r==n)
        return 1;
    return ncr(n-1,r-1)+ncr(n-1,r);
}
int main()
{
    int n,r;
    cout<<"enter n and r";
    cin>>n>>r;
    cout<<"ncr = "<<ncr(n,r)<<endl;
    return 0;
}
